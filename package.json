{
  "name": "@zimtsui/coroutine-locks",
  "version": "0.0.27",
  "description": "",
  "exports": "./build/exports.js",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "rm -rf ./test/build && tsc -b ./test/src/tsconfig.json && ava ./test/build/**/*.js",
    "build": "tsc -b ./src/tsconfig.json",
    "clean": "rm -rf ./build",
    "docs": "rm -rf ./docs && typedoc --tsconfig ./src/tsconfig.json ./src/exports.ts",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/zimtsui/coroutine-locks.git"
  },
  "author": "Zim",
  "devDependencies": {
    "ava": "^7.0.0"
  }
}
